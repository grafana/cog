{{- define "object_dashboard_DataQueryKind_field_spec_custom_strict_unmarshal" -}}
	{{- $cog := importPkg "cog" -}}
	dataquery, err := cog.UnmarshalDataquery(fields["spec"], resource.Kind)
	if err != nil {
		errs = append(errs, cog.MakeBuildErrors("spec", err)...)
	} else {
		resource.Spec = dataquery
	}
{{- end }}
