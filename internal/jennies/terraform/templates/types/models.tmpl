package provider

import (
	"github.com/hashicorp/terraform-plugin-framework/types"
)

{{ $package := .Schema.Package | formatObjectName }}
{{ range .Objects | }}
type {{ formatModelName $package .Name }}DataSourceModel struct {
  {{ range .Type.Struct.Fields | filterScalars }}
     {{.Name | formatFieldName }} {{.Type | formatTerraformType }} `tfsdk:"{{.Name | formatFieldNameTFSDK }}"`
  {{ end }}
}
{{ end }}
