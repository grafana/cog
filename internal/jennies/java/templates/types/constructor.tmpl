{{- define "constructor" }}

    public {{ .Name }}({{ .Name }}Builder builder) {
        {{- range .Fields }}
        this.{{ .Name }} = builder.get{{ camelcase .Name }}();
        {{- end }}
    }

{{- end }}
